---
phase: 02-neutral-inbox
plan: 02
type: execute
autonomous: true
---

<objective>
## Goal

Prove the whole inbound chain through the real HTTP route — publicId validation,
encrypted-secret load, raw-byte HMAC verification, JSON parse, normalization,
ingest, reconciliation and thread projection — instead of only its parts.
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/02-neutral-inbox/02-01-SUMMARY.md
@app/convex/http.ts
@app/convex/_test/hmacVerify.test.ts
@app/convex/_test/channelInbox.test.ts
</context>

<acceptance_criteria>
## AC-1: The real webhook route settles an outbox row end to end
Given a lab channel whose webhook secret is stored as real AES-GCM ciphertext
And an outbox row in "accepted" carrying providerMessageId "wamid.E2E"
When a status.delivered payload is POSTed to
    /provider-webhook/leo-hub/{publicId} with a valid X-Hub-Signature-256
    computed over the exact raw bytes
Then the response is 200 with {"accepted":1,"duplicates":0}
And the outbox row becomes "delivered"
And a channelThreads row exists for that recipient
And the same request replayed returns {"accepted":0,"duplicates":1} without
    changing the outbox row
And a request with a tampered body or wrong secret returns 401 and writes nothing
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Add an end-to-end webhook route test</name>
  <files>app/convex/_test/leoHubWebhookRoute.test.ts</files>
  <action>
    Create the test with a real encryption key set via
    process.env.WABA_TOKEN_ENCRYPTION_KEY_V1 (64 hex chars), seeding the channel
    secret through the real `encryptSecret` so `loadWebhookContext` decrypts it
    for real. Sign bodies with HMAC-SHA256 over the exact bytes sent, reusing the
    `signBody` approach from `hmacVerify.test.ts`. Drive the route with
    `t.fetch`, not by calling `ingestWebhookEvents` directly — calling the
    mutation would bypass exactly what this plan exists to prove.

    Cover: happy path, replay, tampered body, wrong secret, missing signature,
    malformed publicId, and unknown publicId.

    Avoid: changing http.ts, leoHubLab.ts, or any source file. This plan is
    test-only. If the test reveals a real defect, stop and report it rather than
    patching source under a test-only plan.
  </action>
  <verify>cd /Users/sidneychambal/openbsp/app && npm test -- --run convex/_test/leoHubWebhookRoute.test.ts && npm test -- --run && npm run typecheck</verify>
  <done>AC-1 satisfied: the full chain is proven through the real route.</done>
</task>
</tasks>

<output>
After completion, create .paul/phases/02-neutral-inbox/02-02-SUMMARY.md
</output>
